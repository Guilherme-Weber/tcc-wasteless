<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.WasteLess" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primaria</item>
        <item name="colorPrimaryVariant">@color/secundaria</item>
        <item name="colorSecondary">#FB2A2A</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBarOverlay">false</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WasteLess" parent="Base.Theme.WasteLess" />

    <style name="botaoPadrao" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/terciaria</item>
        <item name="colorButtonNormal">@color/secundaria</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>